name: Build
on:
  push:
    paths-ignore:
      - "README.md"
permissions:
  contents: read
jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-16.04, ubuntu-18.04, ubuntu-latest, macos-latest ]
    steps:
      - uses: actions/checkout@v3
      - run: make
